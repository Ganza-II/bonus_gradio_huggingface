# bonus_gradio_huggingface
Tutorial Goal: Build a basic Gradio app using a Blocks layout (rows/columns), an image, text input fields, and a slider. Then integrate a Hugging Face transformers pipeline into the app so user input is processed by a pretrained model and results are displayed in the UI.
